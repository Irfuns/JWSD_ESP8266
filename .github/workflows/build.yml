name: Build JWSD ESP8266 Firmware

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    # 1️⃣ Checkout repository
    - name: Checkout source
      uses: actions/checkout@v4

    # 2️⃣ Setup Arduino CLI
    - name: Setup Arduino CLI
      uses: arduino/setup-arduino-cli@v1

    # 3️⃣ Add ESP8266 Board Manager URL
    - name: Configure ESP8266 board URL
      run: |
        arduino-cli config init
        arduino-cli config set board_manager.additional_urls https://arduino.esp8266.com/stable/package_esp8266com_index.json

    # 4️⃣ Install ESP8266 Core
    - name: Install ESP8266 Core
      run: |
        arduino-cli core update-index
        arduino-cli core install esp8266:esp8266

    # 5️⃣ Install Required Libraries (OFFICIAL ONLY)
    - name: Install Libraries
      run: |
        arduino-cli lib install "ArduinoJson"
        arduino-cli lib install "RTClib"
        arduino-cli lib install "PrayerTimes"
        arduino-cli lib install "NTPClient"
        arduino-cli lib install "DMDESP"

    # 6️⃣ Compile Sketch
    - name: Compile Firmware
      run: |
        arduino-cli compile \
          --fqbn esp8266:esp8266:generic \
          --output-dir build \
          JWSD_ESP8266

    # 7️⃣ Upload BIN Artifact
    - name: Upload Firmware BIN
      uses: actions/upload-artifact@v4
      with:
        name: JWSD_ESP8266_BIN
        path: build/*.bin
